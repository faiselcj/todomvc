version: 0.2

env:
  variables:
    ACCOUNT_ID: "093359840674"
    REGION: "us-east-1"
    REPOSITORY_NAME: "react-app-codebuil"

phases:
  pre_build:
    commands:
      - aws --version
      - export REPOSITORY_URI=${ACCOUNT_ID}.dkr.ecr.${REGION}.amazonaws.com/${REPOSITORY_NAME}
      - export IMAGE_TAG=${CODEBUILD_BUILD_NUMBER}
      - echo ${REPOSITORY_URI}
      - echo ${IMAGE_TAG}
      - aws ecr get-login-password --region ${REGION} | docker login --username AWS --password-stdin ${ACCOUNT_ID}.dkr.ecr.${REGION}.amazonaws.com

  build:
    commands:
      - cd examples/react
      - docker build -t react-app .
      - docker tag react-app:latest ${REPOSITORY_URI}:${IMAGE_TAG}
      - docker tag react-app:latest ${REPOSITORY_URI}:latest

  post_build:
    commands:
      - docker push ${REPOSITORY_URI}:${IMAGE_TAG}
      - docker push ${REPOSITORY_URI}:latest
      - cd ${CODEBUILD_SRC_DIR}
      - printf '[{"name":"react-app","imageUri":"%s"}]' ${REPOSITORY_URI}:${IMAGE_TAG} > imagedefinitions.json

artifacts:
  files:
    - imagedefinitions.json
